The <info>%command.name%</info> command generates a new resource manager.

<info>php %command.full_name% managername</info>

If the argument is missing, the command will ask for the bound class name interactively.